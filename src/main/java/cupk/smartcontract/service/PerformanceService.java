package cupk.smartcontract.service;

/**
 * Legacy service placeholder.
 *
 * <p>The active fulfillment implementation lives in {@link FulfillmentService}.
 * This class is intentionally not a Spring service because the old implementation
 * used an obsolete data model that conflicts with the current fulfillment module.</p>
 */
public class PerformanceService {
}
