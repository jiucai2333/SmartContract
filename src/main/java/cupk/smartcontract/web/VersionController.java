package cupk.smartcontract.web;

/**
 * Legacy controller placeholder.
 *
 * <p>The active version API controller lives in {@code cupk.smartcontract.controller.VersionController}.
 * Keeping this class non-component avoids duplicate /api mappings during local startup.</p>
 */
public class VersionController {
}
