# ==============================
# Application
# ==============================
spring.application.name=SmartContract
server.port=8080
spring.profiles.default=mysql

# ==============================
# Datasource - MySQL
# ==============================
spring.datasource.url=jdbc:mysql://localhost:3306/smart_contract_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=z123

# ==============================
# Jackson
# ==============================
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=GMT+8

# ==============================
# Session
# ==============================
spring.servlet.session.timeout=1800

# ==============================
# Async / SSE Timeout
# ==============================
spring.mvc.async.request-timeout=180000

# ==============================
# MyBatis-Plus
# ==============================
mybatis-plus.configuration.map-underscore-to-camel-case=true
mybatis-plus.global-config.db-config.id-type=auto
mybatis-plus.global-config.db-config.logic-delete-field=deleted
mybatis-plus.global-config.db-config.logic-delete-value=1
mybatis-plus.global-config.db-config.logic-not-delete-value=0

# ==============================
# Logging
# ==============================
logging.level.cupk.smartcontract=debug
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ==============================
# AI - Qwen
# ==============================
# DashScope compatible OpenAI API:
# https://dashscope.aliyuncs.com/compatible-mode/v1
#
# Do not hard-code API keys in Git.
# Set environment variable instead:
# Windows PowerShell:
#   $env:DASHSCOPE_API_KEY="sk-xxx"
ai.qwen.enabled=true
ai.qwen.api-key=${DASHSCOPE_API_KEY:}
ai.qwen.base-url=https://dashscope.aliyuncs.com/compatible-mode/v1
ai.qwen.model=qwen-plus
ai.qwen.vision-model=qwen-vl-plus
ai.qwen.timeout-seconds=120

# ==============================
# OCR
# ==============================
# provider options:
#   paddle          - PaddleOCR service
#   aliyun-openapi  - Aliyun OCR OpenAPI
#   aliyun          - Aliyun OCR service
ai.ocr.enabled=true
ai.ocr.provider=paddle

# PaddleOCR configuration
# Do not hard-code OCR token in Git.
# Set environment variable instead:
# Windows PowerShell:
#   $env:PADDLE_OCR_TOKEN="your-token"
ai.ocr.paddle-token=${PADDLE_OCR_TOKEN:}
ai.ocr.paddle-job-url=https://paddleocr.aistudio-app.com/api/v2/ocr/jobs
ai.ocr.paddle-timeout-seconds=120
ai.ocr.paddle-model=PaddleOCR-VL-1.6

# ==============================
# Local File Storage
# ==============================
storage.local.base-dir=./data/uploads
storage.local.bucket=local

# ==============================
# Multipart Upload
# ==============================
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=210MB

# ==============================
# Contract Approval Threshold
# ==============================
# Major contract threshold: contracts >= 100000 require additional review.
contract.threshold.major=100000

# Super major contract threshold: contracts >= 500000 require higher-level review.
contract.threshold.super=500000
